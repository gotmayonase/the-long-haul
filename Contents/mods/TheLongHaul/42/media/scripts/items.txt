// The Long Haul - Item Definitions
// Build 42.15+
//
// Icons: generated placeholder PNGs in 42/media/textures/
//   Replace with final pixel art when ready; run scripts/generate_icons.py to regenerate placeholders.
//
// Models: vanilla placeholders referenced by model script name.
//   TODO:VERIFY these model names exist in B42 by checking vanilla scripts.
//   TODO: Replace with custom FBX models once created in Blender.
//   Items with no usable vanilla analogue have model properties omitted —
//   they'll be invisible in-hand until custom models are made.

module LongHaul
{
    imports
    {
        Base
    }

    // =========================================================================
    // TRAVOIS LINE
    // Two-handed drag items. High speed penalty, no combat while held.
    // =========================================================================

    // Tier 1 — just branches lashed together. Day-one craft.
    item StickTravois
    {
        DisplayName         = Stick Travois,
        DisplayCategory     = Container,
        ItemType            = base:container,
        Weight              = 2.0,
        Icon                = StickTravois,
        Capacity            = 20,
        WeightReduction     = 50,
        RunSpeedModifier    = 0.35,
        TwoHandWeapon       = TRUE,
        CloseSound          = CloseSack,
        OpenSound           = OpenSack,
        PutInSound          = StoreItemSack,
        // Vanilla placeholder: Sledgehammer is a long two-handed item, closest shape
        // TODO:VERIFY model name; replace with custom model
        StaticModel             = Sledgehammer,
        WorldStaticModel        = Sledgehammer,
        ReplaceInPrimaryHand    = Sledgehammer holding2handedWeapon,
        ReplaceInSecondHand     = Sledgehammer holding2handedWeaponLeft,
        Tags                    = base:holdgeneral,
    }

    // Tier 2 — canvas platform lashed between the poles. More stable load.
    item LashedTravois
    {
        DisplayName         = Lashed Travois,
        DisplayCategory     = Container,
        ItemType            = base:container,
        Weight              = 2.5,
        Icon                = LashedTravois,
        Capacity            = 35,
        WeightReduction     = 60,
        RunSpeedModifier    = 0.40,
        TwoHandWeapon       = TRUE,
        CloseSound          = CloseSack,
        OpenSound           = OpenSack,
        PutInSound          = StoreItemSack,
        StaticModel             = Sledgehammer,
        WorldStaticModel        = Sledgehammer,
        ReplaceInPrimaryHand    = Sledgehammer holding2handedWeapon,
        ReplaceInSecondHand     = Sledgehammer holding2handedWeaponLeft,
        Tags                    = base:holdgeneral,
    }

    // Tier 3 — plank platform on flat runners. End of the drag line.
    // Consumed as a base when crafting either wheelbarrow line.
    item DragSledge
    {
        DisplayName         = Drag Sledge,
        DisplayCategory     = Container,
        ItemType            = base:container,
        Weight              = 5.0,
        Icon                = DragSledge,
        Capacity            = 50,
        WeightReduction     = 65,
        RunSpeedModifier    = 0.45,
        TwoHandWeapon       = TRUE,
        CloseSound          = CloseSack,
        OpenSound           = OpenSack,
        PutInSound          = StoreItemSack,
        // No good vanilla analogue for a flat sled — omit until custom model exists
        // TODO: custom model
        Tags                    = base:holdgeneral,
    }

    // =========================================================================
    // SHOULDER YOKE LINE
    // Balanced across both shoulders. Lower speed penalty than drag items.
    // Good for water/bucket hauling. Both hands occupied but less noise.
    // =========================================================================

    // Tier 1 — carved branch, notched at each end for hanging loads.
    item ShoulderYoke
    {
        DisplayName         = Shoulder Yoke,
        DisplayCategory     = Container,
        ItemType            = base:container,
        Weight              = 1.0,
        Icon                = ShoulderYoke,
        Capacity            = 20,
        WeightReduction     = 40,
        RunSpeedModifier    = 0.85,
        TwoHandWeapon       = TRUE,
        CloseSound          = CloseSack,
        OpenSound           = OpenSack,
        PutInSound          = StoreItemSack,
        // Vanilla placeholder: Plank is a horizontal held item, passable shape
        // TODO:VERIFY model name; replace with custom model
        StaticModel             = Plank,
        WorldStaticModel        = Plank,
        ReplaceInPrimaryHand    = Plank holding2handedWeapon,
        ReplaceInSecondHand     = Plank holding2handedWeaponLeft,
        Tags                    = base:holdgeneral,
    }

    // Tier 2 — padded with rags/leather for longer carries without fatigue.
    item PaddedYoke
    {
        DisplayName         = Padded Shoulder Yoke,
        DisplayCategory     = Container,
        ItemType            = base:container,
        Weight              = 1.2,
        Icon                = PaddedYoke,
        Capacity            = 30,
        WeightReduction     = 50,
        RunSpeedModifier    = 0.90,
        TwoHandWeapon       = TRUE,
        CloseSound          = CloseSack,
        OpenSound           = OpenSack,
        PutInSound          = StoreItemSack,
        StaticModel             = Plank,
        WorldStaticModel        = Plank,
        ReplaceInPrimaryHand    = Plank holding2handedWeapon,
        ReplaceInSecondHand     = Plank holding2handedWeaponLeft,
        Tags                    = base:holdgeneral,
    }

    // =========================================================================
    // WHEEL COMPONENTS
    // Crafting sub-components used in wheelbarrow recipes.
    // Not containers — purely materials.
    // =========================================================================

    // NOTE: Wooden Rod already exists as a vanilla craftable item via Carving.
    // Use Base.WoodenRod (TODO:VERIFY exact ID) in recipes — do not redefine it here.

    // Solid disc carved from a log. Heavier, functional as a standalone wheel.
    // Can be used directly in a wheelbarrow OR as the hub for an assembled wheel.
    item CarvedWoodWheel
    {
        DisplayName         = Carved Wooden Wheel,
        DisplayCategory     = Material,
        ItemType            = base:normal,
        Weight              = 3.0,
        Icon                = CarvedWoodWheel,
        // No good vanilla analogue — omit model until custom art exists
    }

    // Spoked wheel built around a carved hub. Lighter and faster than the solid wheel.
    item AssembledWoodWheel
    {
        DisplayName         = Assembled Wooden Wheel,
        DisplayCategory     = Material,
        ItemType            = base:normal,
        Weight              = 2.0,
        Icon                = AssembledWoodWheel,
        // No good vanilla analogue — omit model until custom art exists
    }

    // =========================================================================
    // STONE WHEELBARROW LINE
    // Masonry path. High capacity and durability, lower speed.
    // Both tiers branch from the Drag Sledge independently of the wooden line.
    // =========================================================================

    // Tier 1 — stone wheel from masonry mounted on a sled frame.
    // Heavy, slow, very durable. The "tank" option.
    item StoneWheelbarrow
    {
        DisplayName         = Stone Wheelbarrow,
        DisplayCategory     = Container,
        ItemType            = base:container,
        Weight              = 8.0,
        Icon                = StoneWheelbarrow,
        Capacity            = 65,
        WeightReduction     = 72,
        RunSpeedModifier    = 0.50,
        TwoHandWeapon       = TRUE,
        CloseSound          = CloseSack,
        OpenSound           = OpenSack,
        PutInSound          = StoreItemSack,
        // TODO: custom model — no vanilla wheelbarrow model confirmed in B42 vanilla
        Tags                    = base:holdgeneral,
    }

    // Tier 2 — metal axle and frame reinforcement. Highest capacity.
    item MetalAxledStoneWheelbarrow
    {
        DisplayName         = Metal-Axled Stone Wheelbarrow,
        DisplayCategory     = Container,
        ItemType            = base:container,
        Weight              = 9.0,
        Icon                = MetalAxledStoneWheelbarrow,
        Capacity            = 90,
        WeightReduction     = 85,
        RunSpeedModifier    = 0.55,
        TwoHandWeapon       = TRUE,
        CloseSound          = CloseSack,
        OpenSound           = OpenSack,
        PutInSound          = StoreItemSack,
        Tags                    = base:holdgeneral,
    }

    // =========================================================================
    // WOODEN WHEELBARROW LINE
    // Carpentry/Carving path. Better speed, good capacity.
    // Both tiers branch from the Drag Sledge independently of the stone line.
    // =========================================================================

    // Tier 1 — wooden wheel on a sled frame. Lighter and faster than stone.
    item WoodenWheelbarrow
    {
        DisplayName         = Wooden Wheelbarrow,
        DisplayCategory     = Container,
        ItemType            = base:container,
        Weight              = 5.0,
        Icon                = WoodenWheelbarrow,
        Capacity            = 70,
        WeightReduction     = 75,
        RunSpeedModifier    = 0.65,
        TwoHandWeapon       = TRUE,
        CloseSound          = CloseSack,
        OpenSound           = OpenSack,
        PutInSound          = StoreItemSack,
        Tags                    = base:holdgeneral,
    }

    // Tier 2 — metal-reinforced frame and axle. Best speed.
    item ReinforcedWheelbarrow
    {
        DisplayName         = Reinforced Wheelbarrow,
        DisplayCategory     = Container,
        ItemType            = base:container,
        Weight              = 5.5,
        Icon                = ReinforcedWheelbarrow,
        Capacity            = 80,
        WeightReduction     = 80,
        RunSpeedModifier    = 0.70,
        TwoHandWeapon       = TRUE,
        CloseSound          = CloseSack,
        OpenSound           = OpenSack,
        PutInSound          = StoreItemSack,
        Tags                    = base:holdgeneral,
    }
}
